let chain = "123";
let num = 123;

console.log(chain + num);
console.log(num + chain);

//Al intentar sumar estas dos variables, el número se convierte en cadena y se concatenan independientemente del orden en el que se posicionen